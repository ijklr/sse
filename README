This is a quick speed comparison of various ways to get dot-product. 
The code is a modified version of 
http://blog.theincredibleholk.org/blog/2012/12/10/optimizing-dot-product/
Just to make it run on OSX and getting rid of some dependencies.

run  `make`
it creates `dot-product` executable.
then run that, the benchmark will start.

C++ `Makefile` compiler flags can be changed, depending on your platform.
